apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: banana-frontend-config
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: "301"
  sslPolicy: banana-ssl-policy