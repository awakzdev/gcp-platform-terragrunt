apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  annotations:
  name: externalsecret-audit
  namespace: mycompany
spec:
  dataFrom:
  - find:
      decodingStrategy: None
      tags:
        environment: banana
        audit-backend: "true"
    rewrite:
    - regexp:
        source: "BANANA_(.*)"
        target: "$1"
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: cluster-gcp-secrets
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
    name: audit-backend
