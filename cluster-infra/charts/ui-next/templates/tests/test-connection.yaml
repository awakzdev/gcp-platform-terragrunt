apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ui-next.fullname" . }}-test-connection"
  labels:
    {{- include "ui-next.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ui-next.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
