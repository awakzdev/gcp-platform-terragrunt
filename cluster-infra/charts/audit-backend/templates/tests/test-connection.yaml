apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "audit-backend.fullname" . }}-test-connection"
  labels:
    {{- include "audit-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "audit-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
