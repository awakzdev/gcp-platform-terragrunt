apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-gcp-project-id.fullname" . }}
  labels:
    {{- include "my-gcp-project-id.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-gcp-project-id.selectorLabels" . | nindent 4 }}
