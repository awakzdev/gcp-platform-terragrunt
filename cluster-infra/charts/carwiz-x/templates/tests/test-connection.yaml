apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-gcp-project-id.fullname" . }}-test-connection"
  labels:
    {{- include "my-gcp-project-id.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-gcp-project-id.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
