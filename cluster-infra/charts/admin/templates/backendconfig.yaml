{{- if .Values.gceBackendConfig.enabled }}
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: {{ include "admin.fullname" . }}-backend-config
spec:
{{- toYaml .Values.gceBackendConfig.spec | nindent 2 }}
  
{{- end }}