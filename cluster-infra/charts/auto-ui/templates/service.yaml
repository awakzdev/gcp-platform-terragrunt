apiVersion: v1
kind: Service
metadata:
  name: {{ include "auto-ui.fullname" . }}
  labels:
    {{- include "auto-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "auto-ui.selectorLabels" . | nindent 4 }}
