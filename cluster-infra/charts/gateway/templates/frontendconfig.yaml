{{- if .Values.gceFrontendConfig.enabled }}
apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: {{ include "gateway.fullname" . }}-gateway
spec:
{{- toYaml .Values.gceFrontendConfig.spec | nindent 2 }}
{{- end }}