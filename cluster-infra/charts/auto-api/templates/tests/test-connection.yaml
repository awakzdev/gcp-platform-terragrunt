apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auto-api.fullname" . }}-test-connection"
  labels:
    {{- include "auto-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auto-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
